+++
id = "T-004"
title = "add ability to reorder items in the sidebar"
status = "done"
priority = 3
created_at = "2026-05-01T16:48:35Z"
updated_at = "2026-05-01T17:04:38Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "635fe9a50f221c3d"
+++
## Notes

- 2026-05-01T16:50:16Z [sarocu] Plan:
- User-defined ordering, persisted to data/order.json as a string[] of slugs. Slugs not in the array fall through, sorted by updatedAt desc (current default), and are appended at the end.
- New src/server/order.ts: getOrder() reads JSON or returns []; setOrder(slugs[]) writes atomically (Bun.write). reconcile(slugs) drops slugs that no longer exist on disk and is called from listDocs.
- src/server/docs.ts: listDocs applies ordering — partition into ordered (in order) + unordered (mtime desc) and concat.
- src/index.ts: new route PUT /api/docs/order { slugs: string[] } -> writes order.
- src/lib/api.ts: api.reorder(slugs).
- src/components/Sidebar.tsx: HTML5 drag-and-drop on .doc-item (draggable, onDragStart stores slug, onDragOver preventDefault, onDrop computes new array, calls onReorder which delegates to api.reorder + refresh). Visual: dashed border on drop target.
- Edge: when a doc is deleted/created, listDocs reconcile keeps order.json consistent.
- Verify: reorder by drag, reload page, confirm order persists; delete a doc, confirm no stale entry remains in order.json; create a new doc, confirm it appears at top of unordered tail.
- 2026-05-01T17:02:23Z [sarocu] Refinement after re-reading src/index.ts: PUT /api/docs/order would be shadowed by the existing /api/docs/:slug route (Bun matches by registration but :slug would also match 'order'). Use a non-slug-collidable path: PUT /api/docs/_order (leading underscore is rejected by SLUG_RE in src/server/docs.ts so it can never collide with a real doc). Also: App.tsx needs a handleReorder callback wired to Sidebar via new onReorder prop; refreshDocs after api.reorder so order.json reconcile runs.
- 2026-05-01T17:04:38Z [sarocu] Implemented:
- src/server/order.ts: getOrder/setOrder/reconcile, persists data/order.json
- src/server/docs.ts: listDocs partitions ordered (in saved order) + remainder (mtime desc)
- src/index.ts: PUT /api/docs/_order route registered before :slug; SLUG_RE rejects leading underscore so no collision
- src/lib/api.ts: api.reorder(slugs)
- src/components/Sidebar.tsx: native HTML5 drag/drop on doc-item; drop reorders array and calls onReorder
- src/App.tsx: handleReorder optimistic-updates docs then api.reorder + refresh
- src/index.css: .doc-item.dragging (opacity), .drop-target (dashed outline)
Verified: bunx tsc --noEmit clean (pre-existing tsconfig baseUrl deprecation only); bun build succeeds.
