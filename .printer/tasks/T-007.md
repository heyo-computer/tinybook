+++
id = "T-007"
title = "styling: add paper shaders to provide texture to the app everywhere except the editor components: https://shaders.paper.design/halftone-dots#colorBack=f2f1e8&colorFront=2b2b2b&originalColors=false&type=gooey&grid=hex&inverted=false&size=0.5&radius=1.25&contrast=0.4&grainMixer=0.2&grainOverlay=0.2&grainSize=0.5&scale=1&fit=cover"
status = "done"
priority = 3
created_at = "2026-05-01T16:48:35Z"
updated_at = "2026-05-01T17:10:15Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "93a52bad98343950"
+++
## Notes

- 2026-05-01T16:50:40Z [sarocu] Plan:
- Use @paper-design/shaders-react (HalftoneDots). Install: bun add @paper-design/shaders-react. Confirm the package name in npm before installing; if unavailable use the canvas-based @paper-design/shaders core package instead.
- New src/components/PaperBackdrop.tsx: wraps <HalftoneDots /> sized to fill its parent (position:absolute, inset:0, pointer-events:none, z-index:0). Props mirror the shader URL params: colorBack='#f2f1e8', colorFront='#2b2b2b', type='gooey', grid='hex', size=0.5, radius=1.25, contrast=0.4, grainMixer=0.2, grainOverlay=0.2, grainSize=0.5, scale=1, fit='cover'.
- Place <PaperBackdrop /> as first child inside .sidebar, .chat, and any modal/pin overlays — NOT inside .main / .editor / .editor-* (spec exclusion).
- index.css: ensure .sidebar, .chat are position:relative; child content (header, lists) needs z-index:1 + position:relative so it stacks above the shader. Keep .main background unchanged.
- The light shader colors (cream + dark dots) clash with the current dark palette. Update affected panel CSS variables locally so text remains readable on the cream backdrop (e.g. add --panel-fg overrides scoped to .sidebar, .chat). Confirm contrast in browser.
- Verify: bun --hot src/index.ts; visually confirm sidebar+chat show halftone backdrop and editor remains plain; check no perf regression on doc selection.
- 2026-05-01T17:10:15Z [sarocu] Implemented:
- Installed @paper-design/shaders-react@0.0.76.
- src/components/PaperBackdrop.tsx: HalftoneDots wrapped in absolute-inset div, params match the shader URL (colorBack #f2f1e8, colorFront #2b2b2b, type=gooey, grid=hex, size=0.5, radius=1.25, contrast=0.4, grain* values, scale=1, fit=cover, originalColors/inverted=false), aria-hidden, pointer-events:none.
- Sidebar.tsx + Chat.tsx: render <PaperBackdrop /> as first child. Editor/main untouched per spec exclusion.
- index.css: .paper-backdrop positioning + z-index:0; scoped variable overrides on .sidebar/.chat to flip text/muted/accent/border/panel-2 to dark-on-cream so content remains readable on the light shader. Adjusted chat-msg.user (dark on cream text), chat-input textarea (cream w/ dark text), and .chat .chat-content backgrounds for legibility.
Verified: bun build --target bun OK; bun src/index.ts serves HTTP 200, bundle compiles in ~36ms with no errors. Visual contrast not confirmed in a live browser (sandbox).
