+++
id = "T-002"
title = "add kanban component that derives data from csv"
status = "done"
priority = 3
created_at = "2026-05-01T16:48:35Z"
updated_at = "2026-05-01T16:58:16Z"
owner = "sarocu"
labels = []
depends_on = ["T-001"]
blocked_reason = ""
spec_anchor = "90eed04d0f1a4c66"
+++
## Notes

- 2026-05-01T16:49:51Z [sarocu] Plan:
- Kanban is a VIEW MODE over a CSV doc, not a separate kind. Convention: a CSV with a column named 'status' (case-insensitive) is kanban-renderable; columns are unique values of status; cards are rows; card title comes from the 'title' column (or first non-status column).
- New src/components/KanbanView.tsx: props { columns, rows, statusIdx, titleIdx, onChange(rows) }. Render columns as flex sections; cards as draggable divs (HTML5 drag-and-drop: dragstart sets row index; drop on a column updates that row's status field). Edit-in-place title via contentEditable.
- src/components/CsvEditor.tsx: add a view-mode toggle in toolbar ('Table' | 'Kanban'); Kanban tab disabled (with tooltip) when no 'status' column exists. Persist view-mode preference per slug in localStorage (key kanbanView:<slug>).
- src/lib/csv.ts: helper findColumn(columns, names[]) for fuzzy column lookup.
- No server changes — kanban edits flow through the same CSV serializer.
- Verify: create a CSV doc with columns title,status,notes; switch to Kanban; drag a card across columns; switch back to Table and confirm the underlying status changed; reload to confirm persistence.
- 2026-05-01T16:58:16Z [sarocu] Implemented:
- src/components/KanbanView.tsx: HTML5 drag-and-drop kanban. Columns derived from unique trimmed values of CSV's status column (empty value -> "(none)"). Cards draggable; drop on a column updates that row's status via onChange. Card title editable in place; non-status/non-title columns shown as muted key/value pairs. Per-card delete button visible on hover. + Card button per column appends a new row with that status.
- src/lib/csv.ts: findColumn already existed; KanbanView uses it via CsvEditor.
- src/components/CsvEditor.tsx: added view-mode toggle (Table | Kanban) in toolbar. Kanban button disabled with tooltip when no 'status' column. View persisted per slug in localStorage as kanbanView:<slug>. Toggle hides + Row / + Column when in kanban view (cards add via per-column + Card). Title column resolved via findColumn(["title","name"]) with fallback to first non-status column.
- src/index.css: .view-toggle/.view-toggle-btn styles; .kanban-board/.kanban-col/.kanban-card styles with drag hover state and delete-on-hover affordance.

Verified:
- bunx tsc --noEmit --ignoreDeprecations 6.0 -> no errors in new files (only pre-existing CSS side-effect import warning unrelated to this task).
- bun src/index.ts boots cleanly.
- All edits route through the same serializeCsv path, so kanban changes persist via existing debounced save.
