+++
id = "T-010"
title = 'combine the "New Document" and "import" buttons into a single button with a "+" icon that displays a modal that allows the user to create a specific doc type or import'
status = "done"
priority = 3
created_at = "2026-05-01T21:56:36Z"
updated_at = "2026-05-01T22:04:20Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "df4b9cf16a3eb7b4"
+++
## Notes

- 2026-05-01T21:57:25Z [sarocu] Plan: unify "+ New document" + "Import" into a single "+" button that opens a creation modal.

Files:
- src/components/Sidebar.tsx:
  - Remove the dropdown menu state (menuOpen, wrapRef, pick) and the existing import-btn + hidden file input from the new-doc-row (keep file input ref but move into new modal).
  - Replace with a single icon-style button with `+` that calls a new `onCreatePrompt()` prop OR opens a local modal.
  - Prefer: lift modal to a new component `src/components/CreateDocModal.tsx` (sibling to SettingsModal). Sidebar just toggles open state.
- New file src/components/CreateDocModal.tsx:
  - Modal lists three "create" tiles (Markdown / CSV / Kanban) and an "Import file(s)" tile/section.
  - Wires to existing onNew(kind) and onImport(files) flows.
  - Closes after action; surfaces import skipped warning inside modal.
- src/App.tsx: wire new modal open state, or pass through to Sidebar to keep stateless. Simpler: keep state in Sidebar (matches current dropdown pattern).
- src/index.css: add `.create-modal` styles; reuse `.modal` shell. Style the `+` trigger as `primary-btn icon-btn` with larger plus glyph.

Verification:
- Click +, confirm modal opens with three create options + import button. Each path produces expected doc and closes modal. Skipped-file message renders.
