+++
id = "T-013"
title = "in markdown editor, add a transcribe button that uses mistral voxtral to write a doc from speech"
status = "done"
priority = 3
created_at = "2026-05-01T21:56:36Z"
updated_at = "2026-05-01T22:07:54Z"
owner = "sarocu"
labels = []
depends_on = ["T-012"]
blocked_reason = ""
spec_anchor = "9fbcc533431d8ef2"
+++
## Notes

- 2026-05-01T21:57:55Z [sarocu] Plan: transcribe button in markdown editor that uses Mistral Voxtral.

Depends on T-012 (Mistral provider must exist in settings). Add depends edge.

Files:
- src/components/Editor.tsx (MarkdownEditor):
  - Add a "🎙 Transcribe" toolbar button next to Download. State: `recording`, `transcribing`.
  - On click: getUserMedia({audio:true}) → MediaRecorder (mimeType audio/webm or audio/ogg). Click again to stop. On stop, build a Blob and POST to /api/agent/transcribe with FormData (file + provider creds).
  - On response: insert transcribed text into draft (append at cursor or end). Trigger autosave (already debounced).
- New file src/server/transcribe.ts:
  - export async function transcribe({audio: Blob|Buffer, apiKey, model, baseUrl}) → string.
  - POST multipart/form-data to `${baseUrl||https://api.mistral.ai/v1}/audio/transcriptions` with `file`, `model` (default `voxtral-mini-latest`). Return `text` from JSON.
- src/index.ts: add route `/api/agent/transcribe` POST. Read multipart, pull provider creds from form fields (apiKey, model, baseUrl) — match Chat pattern of sending creds per-request. Require auth via requireAuth.
- src/lib/api.ts: add `transcribe(file: Blob, {apiKey, model, baseUrl})` returning {text}.
- src/components/SettingsModal.tsx: ensure Mistral provider has voxtral model selectable; or pull voxtral creds from mistral provider settings regardless of active provider (since chat may be on Inception while transcribe is Mistral). Decision: transcribe always uses the mistral provider config, error if not configured.

Verification:
- Configure Mistral, click record in markdown editor, speak 5s, click stop, confirm transcribed text appended and saved.
- Without mic permission: graceful error toast. Without Mistral configured: prompt "Configure Mistral in Settings".

Sub-task candidate: server-side /api/agent/transcribe + lib/api wrapper could be split out if scope feels heavy — leaving as one task for now, will split if implementation proves it.
