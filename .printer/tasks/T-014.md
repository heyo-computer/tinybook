+++
id = "T-014"
title = 'in markdown editor, add a "polish" button that asks the agent to clean up the document and potentially compact it'
status = "done"
priority = 3
created_at = "2026-05-01T21:56:36Z"
updated_at = "2026-05-01T22:09:28Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "9f35bc8692274089"
+++
## Notes

- 2026-05-01T21:58:08Z [sarocu] Plan: "Polish" button in markdown editor that asks the agent to clean up + compact the document.

Files:
- src/components/Editor.tsx (MarkdownEditor):
  - Add toolbar button "✨ Polish" next to Download. State: `polishing`.
  - On click: call new api.polish(slug, settings) → updated content. Replace draft with returned content (which triggers debounced save) OR rely on server-side write and refetch — prefer server-side write so doc updates atomically; then optimistically setDraft + setSavedAt.
  - Disable while polishing; surface error inline.
- src/server/agent.ts (or new src/server/polish.ts):
  - Export `polishDoc(slug, {apiKey, model, baseUrl})`. Reads current content, sends a single non-tool chat completion with system prompt: "Polish and lightly compact the following markdown. Preserve meaning, headings, code blocks. Improve flow, fix grammar, dedupe, tighten verbose passages. Return ONLY the revised markdown, no commentary." User message = doc content. Writes result via writeDoc(slug, polished).
- src/index.ts: add POST /api/docs/:slug/polish behind requireAuth. Body: {apiKey, model, baseUrl}. Returns updated Doc.
- src/lib/api.ts: add `polish: (slug, {apiKey, model, baseUrl}) => Doc`.
- src/App.tsx: pass settings into Editor (currently Editor only knows doc + onSave); thread `settings` prop or introduce a `onPolish(slug)` callback in App that has settings closure. Prefer: add `onPolish` to Props in Editor that App provides.

Verification:
- Open a verbose md doc, click Polish, confirm content updates and saves (timestamp moves), preview re-renders.
- Without provider configured: error message routes user to Settings (same pattern as Chat).

Edge cases:
- Content > model context: leave as-is for now; the doc store is small. Note in code only if non-obvious.
- Don't run polish on empty drafts (button disabled when draft.trim() === "").
