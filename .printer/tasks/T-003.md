+++
id = "T-003"
title = "add ability to add csv and kanban documents in sidebar"
status = "done"
priority = 3
created_at = "2026-05-01T16:48:35Z"
updated_at = "2026-05-01T17:01:10Z"
owner = "sarocu"
labels = []
depends_on = ["T-001"]
blocked_reason = ""
spec_anchor = "cb4336053d4d1444"
+++
## Notes

- 2026-05-01T16:49:59Z [sarocu] Plan:
- Replace prompt-based 'New document' in Sidebar.tsx with a dropdown menu: 'New Markdown', 'New CSV', 'New Kanban'. ('Kanban' creates a CSV seeded with header 'title,status,notes\n' — same kind as CSV, but template makes Kanban view immediately useful.)
- src/components/Sidebar.tsx: replace single .primary-btn with a small popover (controlled useState; click outside to close). Items call onNew(kind, template) where kind is 'md'|'csv' and template optionally provides seed content.
- src/App.tsx: handleNew(kind, template) -> prompt for title -> api.create(title, kind, template).
- src/lib/api.ts + src/server/docs.ts + src/index.ts: changes already covered in T-001 plan (kind+content on create).
- Sidebar item: render small kind badge (e.g. 'MD'/'CSV') next to .doc-slug for clarity.
- Verify: open dropdown, create one of each kind, confirm files land in data/docs with correct extension and the right editor opens.
Depends on T-001 (CSV kind plumbing).
- 2026-05-01T17:01:10Z [sarocu] Implemented:
- Sidebar.tsx: 'New document' button now toggles a dropdown with three items (Markdown / CSV / Kanban). Click-outside closes via useEffect+ref.
- Each menu item calls onNew(kind) where kind is 'md'|'csv'|'kanban'.
- Sidebar rows now render a small kind badge (MD/CSV) next to the slug under the title.
- App.tsx: handleNew accepts kind, prompts for title, maps 'kanban' to docKind='csv' with explicit 'title,status,notes\n' template; 'csv' uses server default; 'md' uses default.
- index.css: added .new-doc-wrap/.new-doc-menu/.new-doc-item dropdown styles, .kind-badge, .doc-meta wrapper.
Typecheck passes (pre-existing CSS side-effect import warning unrelated).
