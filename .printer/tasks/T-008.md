+++
id = "T-008"
title = "add dark and light mode theming - the sidebars and the content area should both respect this and be consistent"
status = "done"
priority = 3
created_at = "2026-05-01T21:56:36Z"
updated_at = "2026-05-01T22:02:46Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "5bb1fc610479a7a4"
+++
## Notes

- 2026-05-01T21:57:10Z [sarocu] Plan: dark/light theming consistent across sidebars + content area.

Files:
- src/index.css: introduce light/dark token sets via [data-theme="light"] / [data-theme="dark"] on <html>. Move all hardcoded colors (#1a1a1a, #2b2b2b, rgba overrides in `.sidebar, .chat`, kanban-col, csv-table, editor-textarea, modal, pin) to CSS vars. Add light palette (paper cream bg, dark ink text) and dark palette (current values).
- src/components/PaperBackdrop.tsx: accept theme; switch colorBack/colorFront based on theme (light: f2f1e8/2b2b2b; dark: panel/accent). Read theme via context or prop.
- src/App.tsx: read theme from settings (extend AgentSettings or split into UiSettings) and apply `data-theme` to document.documentElement on mount/change.
- src/components/SettingsModal.tsx: add theme select (system | light | dark). Persist alongside other settings under same STORAGE_KEY (extend type) OR a separate `tinybook.ui.settings` key — prefer extending current settings type with optional `theme` field to keep one source.
- "system" option: detect via matchMedia("(prefers-color-scheme: dark)") and listen for changes when system selected.

Verification:
- bun --hot ./src/index.ts and toggle theme in Settings; check sidebar, editor (md + csv + kanban), chat, modal, pin entry all flip correctly with no contrast issues.
- Confirm PaperBackdrop swaps colors in both panels.
