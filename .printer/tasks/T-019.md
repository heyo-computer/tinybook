+++
id = "T-019"
title = "add context compaction, clear context, and current context to the button group"
status = "done"
priority = 3
created_at = "2026-05-01T22:38:53Z"
updated_at = "2026-05-01T22:42:44Z"
owner = "sarocu"
labels = []
depends_on = ["T-018"]
blocked_reason = ""
spec_anchor = "5efca6b83b488ccf"
+++
## Notes

- 2026-05-01T22:39:57Z [sarocu] Plan: depends on T-018 (button group must exist).

- "Current context" indicator
  - Compute a rough token estimate locally: sum of `m.content.length / 4` plus tool messages. Render as `~Nk` text in the group (read-only).
- "Clear context" button
  - Calls `setHistory([])` and resets any `mentions` state. Confirm via `confirm("Clear chat history?")`.
- "Compact context" button
  - New endpoint: `POST /api/agent/compact` taking `{ messages, apiKey, model, baseUrl }` → returns `{ summary: string }`.
  - Implementation in src/server/agent.ts: call provider with a one-shot prompt asking for a concise bullet summary of prior turns; return summary text.
  - Client replaces history with `[{ role: "system", content: "Prior conversation summary:\n" + summary }]` (or as the first `user` message) so the model retains gist while shrinking tokens.
  - Add `api.compact` in src/lib/api.ts.
- src/components/Chat.tsx wires all three. Disable Compact/Clear when `history.length === 0`.
- Verify: build, send several messages, click Compact and confirm history collapses to a summary; Clear empties; counter updates live.
