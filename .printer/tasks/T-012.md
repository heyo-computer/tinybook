+++
id = "T-012"
title = "add mistral provider to the settings"
status = "done"
priority = 3
created_at = "2026-05-01T21:56:36Z"
updated_at = "2026-05-01T22:06:36Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "a84b65a5631d3da9"
+++
## Notes

- 2026-05-01T21:57:42Z [sarocu] Plan: add Mistral as a second provider in agent settings.

Files:
- src/components/SettingsModal.tsx:
  - Extend AgentSettings: `provider: "inception" | "mistral"` (default "inception"). Persist provider; keep a per-provider `apiKey` and `model` so switching doesn't wipe state — prefer storing as `providers: { inception: {apiKey, model, baseUrl?}, mistral: {...} }, active: "inception"`.
  - Add a provider <select> at top of modal; show only the active provider's fields below. Mistral default model list: `mistral-large-latest`, `mistral-small-latest`, `voxtral-mini-latest`. Default base URL: `https://api.mistral.ai/v1`.
  - Update loadSettings/saveSettings to migrate old shape → new shape (best-effort: treat existing top-level fields as inception).
- src/lib/api.ts: chat() body shape passes resolved {apiKey, model, baseUrl} — no schema change needed; resolution happens in Chat.tsx via active provider.
- src/components/Chat.tsx: resolve active provider's apiKey/model/baseUrl before calling api.chat.
- src/server/agent.ts: already provider-agnostic (OpenAI-compatible chat completions). Mistral's /v1/chat/completions speaks the same shape including tool_calls, so no server change required. Confirm by reading docs if uncertain; otherwise proceed.

Verification:
- Switch provider in Settings, send a chat message, confirm 200 from Mistral. Switch back to Inception and re-send.
- Old saved settings still load (migration path).
