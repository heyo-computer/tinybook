+++
id = "T-009"
title = 'add settings for the title - replace "tinybook" with the title if set'
status = "done"
priority = 3
created_at = "2026-05-01T21:56:36Z"
updated_at = "2026-05-01T22:03:28Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "01b9d90b353fcd47"
+++
## Notes

- 2026-05-01T21:57:15Z [sarocu] Plan: settable workspace title replaces "tinybook" header.

Files:
- src/components/SettingsModal.tsx: add `title?: string` to AgentSettings (or split into a UiSettings; prefer extending existing type to avoid second storage key). Add input "Workspace title" with placeholder "tinybook".
- src/components/Sidebar.tsx: replace hardcoded `<h2>tinybook</h2>` (line 89) with prop `title` defaulting to "tinybook".
- src/App.tsx: pass `settings?.title || "tinybook"` to Sidebar; also update `document.title` via useEffect when settings load/change.

Verification:
- Set title in Settings, confirm sidebar header + browser tab update; clearing reverts to "tinybook".
