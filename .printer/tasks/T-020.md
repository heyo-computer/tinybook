+++
id = "T-020"
title = "add a readonly preview feature which lets anyone read the contents of the book but no agent access and no changes can be made. Ensure that the API endpoints are appropriately guarded"
status = "done"
priority = 3
created_at = "2026-05-01T22:38:53Z"
updated_at = "2026-05-01T22:54:05Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "845269b1ddfe962d"
+++
## Notes

- 2026-05-01T22:40:18Z [sarocu] Plan: read-only preview mode (anyone can view contents; no agent, no writes).

- src/server/auth.ts
  - Add a separate read-only mechanism. Two options — pick (a):
    (a) Generate a `READ_PIN` (or `READ_TOKEN`) on startup alongside `PIN`; log it. Sessions get a `role: "owner" | "reader"`.
    (b) Or env-var configurable `TINYBOOK_READ_TOKEN`.
  - Replace `sessions: Map<string, number>` with `Map<string, { expiry, role }>`. Update `createSession(role)`, `verifySession()` to return the role (or null). Add `verifySessionRole(id): "owner" | "reader" | null`.
- src/index.ts
  - `requireAuth` returns `{ role }` when authorized.
  - Add `requireOwner(req)` helper for write/agent endpoints; readers get 403.
  - Endpoint matrix:
    - GET /api/docs, GET /api/docs/:slug → owner OR reader.
    - POST/PUT/DELETE /api/docs*, PUT /api/docs/_order, /polish, /transcribe, /agent/chat, /agent/compact → owner only.
  - `/api/auth` accepts either pin: tries owner first, else reader; returns `{ sessionId, role }`.
- src/components/PinEntry.tsx + src/lib/api.ts
  - Capture `role` from auth response; expose `getRole()`.
- src/App.tsx
  - When `role === "reader"`: hide Sidebar's "+", download/delete/reorder controls; hide `<Chat>` panel entirely; render Editor in read-only mode (pass `readOnly` prop). Settings modal: hide provider config (or hide whole gear).
- src/components/Editor.tsx, CsvEditor.tsx, KanbanView.tsx
  - Accept `readOnly?: boolean`; disable inputs, hide Save/Polish/Transcribe buttons.
- src/components/Sidebar.tsx
  - Accept `readOnly?: boolean`; gate destructive icons + drag handlers.
- Verify: log in with reader pin → docs visible, all writes return 403 (curl with reader token), UI lacks edit affordances; owner pin retains full functionality.
- 2026-05-01T22:45:35Z [sarocu] Refinement after re-reading auth.ts, index.ts, App.tsx, Sidebar.tsx, Editor.tsx, lib/api.ts:

- src/server/auth.ts:
  - Add `READ_PIN: string` (separate 6-digit), log it on startup alongside PIN.
  - Change sessions map value to `{ expiry: number; role: "owner" | "reader" }`.
  - `createSession(role)` returns sessionId. `verifySession(id)` → `{ role } | null` (callers update accordingly).
- src/index.ts:
  - `requireAuth(req)` returns `{ ok: true; role } | { ok: false; response }`.
  - Add `requireOwner(req)` wrapper that 403s readers.
  - Endpoint matrix:
    - reader-allowed: GET /api/docs, GET /api/docs/:slug
    - owner-only: POST /api/docs, PUT/DELETE /api/docs/:slug, PUT /api/docs/_order, /api/docs/:slug/polish, /api/agent/transcribe, /api/agent/chat, /api/agent/compact
  - /api/auth: try owner pin first, else reader pin; return `{ sessionId, role }`. 401 only if neither matches.
- src/lib/api.ts: store role alongside sessionId; expose `getRole()`. `setSession({ id, role })`.
- src/components/PinEntry.tsx: capture `{ sessionId, role }` from response; pass role up.
- src/App.tsx:
  - `authenticated` becomes `{ role } | null`.
  - When reader: don't render `<Chat>`; pass `readOnly` to Sidebar and Editor; hide the chat resizer; settings gear can stay (theme/title only) but hide provider key fields when reader (or just omit the gear — simpler).
- src/components/Sidebar.tsx: add `readOnly?: boolean`. When true, hide the `+`, settings, download/delete buttons; disable `draggable` on items.
- src/components/Editor.tsx + CsvEditor.tsx + KanbanView.tsx: add `readOnly?: boolean`. Disable `<textarea>`/inputs (`readOnly` attribute), hide Polish/Transcribe/Save toolbar buttons. Keep download visible (it's harmless for readers — confirm acceptable; if not, hide).
- Verify:
  - `bun run`, log in with reader pin → docs visible, no edit affordances, chat hidden.
  - `curl -X POST -H "x-session-id: <reader>" /api/docs ...` returns 403.
  - `curl -H "x-session-id: <reader>" /api/docs` returns 200.
  - Owner pin retains all functionality.
- 2026-05-01T22:54:04Z [sarocu] Implemented read-only preview role:
- auth.ts: separate READ_PIN; sessions track {expiry, role}; verifySession returns {role}|null. Both PINs logged at startup.
- index.ts: requireAuth returns role; new requireOwner 403s readers. Endpoint matrix — readers: GET /api/docs, GET /api/docs/:slug; owner-only: POST/PUT/DELETE /api/docs*, /api/docs/_order, /api/docs/:slug/polish, /api/agent/{chat,compact,transcribe}. /api/auth tries owner PIN then reader PIN; returns {sessionId, role}.
- lib/api.ts: setSession({id,role}) + getRole().
- PinEntry.tsx: returns {sessionId, role}.
- App.tsx: tracks role; readers don't render Chat or SettingsModal; chat resizer hidden; passes readOnly to Sidebar/Editor; chat-w pinned to 0 in readonly.
- Sidebar.tsx: readOnly hides + and gear, removes drag handlers and per-item delete (download stays for readers).
- Editor.tsx: readOnly disables save effect, hides Transcribe/Polish, switches preview pane to full-width centered render.
- CsvEditor.tsx: readOnly disables autosave, makes inputs readOnly, hides +Row/+Column/delete column/row.
- KanbanView.tsx: readOnly disables drag handlers, hides delete card and + Card; titles become readOnly.
- index.css: .editor-panes.readonly + .editor-preview.full styles.

Verified end-to-end with curl against bun --hot:
  reader: GET /api/docs → 200, POST /api/docs → 403, POST /api/agent/chat → 403, PUT /api/docs/_order → 403
  owner:  POST /api/docs → 201
  no session: GET /api/docs → 401
  invalid pin → 401
tsc clean, bun build OK.
