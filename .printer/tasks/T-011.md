+++
id = "T-011"
title = "allow the different panes to be resized"
status = "done"
priority = 3
created_at = "2026-05-01T21:56:36Z"
updated_at = "2026-05-01T22:05:35Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "bc00b8f367c7c898"
+++
## Notes

- 2026-05-01T21:57:33Z [sarocu] Plan: resizable panes for sidebar | main | chat.

Files:
- src/index.css:
  - Replace `.app { grid-template-columns: 260px 1fr 360px }` with CSS vars `--sidebar-w` and `--chat-w`, default 260px / 360px, min/max clamps.
  - Add `.resizer` class: 4px-wide vertical handle, cursor: col-resize, hover highlight.
- src/App.tsx:
  - Add state `sidebarW` / `chatW` (numbers) initialized from localStorage `tinybook.ui.layout` or defaults.
  - Render two `<div className="resizer">` between sidebar/main and main/chat.
  - Pointer-down on a resizer captures pointer, listens to pointermove to update the corresponding width (clamp e.g. [180, 480] sidebar, [240, 600] chat), persists to localStorage on pointerup.
  - Apply widths via inline style on the `.app` container: `style={{ "--sidebar-w": sidebarW + "px", "--chat-w": chatW + "px" }}`.
- Update `.app` grid template to `var(--sidebar-w) 4px 1fr 4px var(--chat-w)`.

Verification:
- Drag both handles; widths persist after reload. Min/max clamps prevent collapse. Editor + chat content reflows without layout breakage.
