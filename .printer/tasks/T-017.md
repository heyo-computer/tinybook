+++
id = "T-017"
title = "ensure there is a web search tool for the agent; add to settings and use tavily for searches"
status = "done"
priority = 3
created_at = "2026-05-01T22:38:53Z"
updated_at = "2026-05-01T22:49:14Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "c42433d6ae522787"
+++
## Notes

- 2026-05-01T22:39:41Z [sarocu] Plan:
- src/components/SettingsModal.tsx
  - Add a third provider-like section "Web search (Tavily)" (separate from the chat `Provider` switch since it's not a chat model). Fields: `tavilyApiKey`. Persist as a sibling of `providers` on `AgentSettings` (e.g. `tools: { tavily?: { apiKey: string } }`).
  - Update `loadSettings`/`saveSettings` to round-trip the new field; handle legacy payloads (treat missing as undefined).
  - Export a helper `tavilyKey(s)` returning `string | null`.
- src/server/tools/tavily.ts (new)
  - `export async function tavilySearch({ apiKey, query, maxResults }): Promise<{ results: { title; url; content }[] }>` calling `https://api.tavily.com/search` with `{ api_key, query, max_results, search_depth: "basic" }`.
- src/server/agent.ts
  - Add a `web_search` tool entry (parameters: `{ query: string, max_results?: number }`). In `runTool`, dispatch to `tavilySearch` when `req.tavilyApiKey` is set; if not set, return `{ error: "tavily not configured" }` so the model surfaces it.
  - Extend `AgentRequest` with `tavilyApiKey?: string` and only include the tool in `TOOLS` when present.
- src/lib/api.ts: add `tavilyApiKey` to the `chat` body shape.
- src/components/Chat.tsx: pull `tavilyApiKey` from settings via `tavilyKey(settings)` and forward in `api.chat`.
- src/index.ts: pass-through is automatic (body spread to `runAgent`); no change needed if shape matches.
- Verify: configure key in Settings, ask "search the web for ..."; confirm tool call + content in reply.
- 2026-05-01T22:45:18Z [sarocu] Refinement after re-reading SettingsModal.tsx + agent.ts:

- AgentSettings shape currently is { active, providers: { inception, mistral } }. Add an optional sibling `tools?: { tavily?: { apiKey: string } }`. Update `loadSettings` to round-trip it (default undefined; legacy payloads stay valid). Add `saveSettings` no-op (already serializes whole object).
- Add `tavilyKey(s: AgentSettings | null): string | null` exported from SettingsModal.tsx.
- SettingsModal UI: below the provider section, render a `<fieldset>` "Web search (Tavily)" with a single `<label>API key</label>` (password input). Persist into `tools.tavily.apiKey` on submit (trim; undefined when empty).
- New file src/server/tools/tavily.ts: `export async function tavilySearch({ apiKey, query, maxResults = 5 })` POST https://api.tavily.com/search with `{ api_key, query, max_results: maxResults, search_depth: "basic" }`. Return parsed `{ results: [{ title, url, content }] }`. Throw on non-2xx.
- agent.ts:
  - Extend `AgentRequest` with `tavilyApiKey?: string`.
  - Build `TOOLS` dynamically: const base = [...]; if (req.tavilyApiKey) push { name: "web_search", parameters: { query: string, max_results?: number } }. Pass that array (not the const) into the request body.
  - In `runTool` add a `case "web_search"` calling `tavilySearch`. Return JSON-stringified results trimmed to ~10k chars total.
- src/lib/api.ts: add `tavilyApiKey?: string` to chat() body type.
- Chat.tsx: import `tavilyKey`; pass `tavilyApiKey: tavilyKey(settings) ?? undefined` to `api.chat`.
- src/index.ts /api/agent/chat: extend body type to include `tavilyApiKey?: string`; spread continues to work since runAgent reads it directly.
- Verify: set Tavily key in Settings, ask "search the web for bun runtime news", confirm tool call + content appears in reply. Without key, the tool is absent and the model says it can't.
- 2026-05-01T22:49:13Z [sarocu] Implemented: SettingsModal AgentSettings.tools.tavily.apiKey + tavilyKey() helper + UI fieldset 'Web search (Tavily)' with password input. New src/server/tools/tavily.ts wraps POST https://api.tavily.com/search. agent.ts AgentRequest gains tavilyApiKey; TOOLS array conditionally includes web_search; runTool dispatches and trims results to ~10k chars. Chat.tsx forwards tavilyKey(settings) on chat send. lib/api.ts + index.ts body types updated. Verified: tsc clean, bun build OK.
