+++
id = "T-001"
title = "add csv editor component"
status = "done"
priority = 3
created_at = "2026-05-01T16:48:35Z"
updated_at = "2026-05-01T16:55:56Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "5754dc072b54fc08"
+++
## Notes

- 2026-05-01T16:49:41Z [sarocu] Plan:
- Generalize doc storage to support multiple kinds. In src/server/docs.ts: add DOC_EXTS = ['md','csv']; pathFor takes (slug, ext); listDocs scans for any of the extensions; readDoc/writeDoc/deleteDoc derive ext from filename on disk (one slug = one ext). DocMeta gains `kind: 'md'|'csv'`. createDoc accepts `kind` and seeds appropriate body ('# Title\n\n' for md; CSV header row for csv).
- src/lib/api.ts: extend Doc/DocMeta with `kind`. api.create takes (title, kind, content?).
- src/index.ts: POST /api/docs accepts `kind` in body.
- New src/components/CsvEditor.tsx: parse CSV with a tiny RFC-4180 parser in src/lib/csv.ts (split into rows respecting quoted fields, escape commas/quotes/newlines on serialize). State = { columns: string[], rows: string[][] }. Render an editable table: contentEditable th + td, '+ Row' / '+ Column' buttons, per-row delete. Debounced onSave (600ms) like Editor.tsx, calling onSave(slug, serialized).
- src/App.tsx + src/components/Editor.tsx: rename current Editor body to MarkdownEditor; new Editor dispatches by doc.kind to MarkdownEditor or CsvEditor.
- Styling: reuse editor-toolbar; add minimal .csv-table styles in index.css.
- Verify: bun --hot src/index.ts; create a .csv doc via API curl, edit in browser, confirm round-trip preserves quoted fields with commas.
- 2026-05-01T16:55:56Z [sarocu] Implemented:
- src/server/docs.ts now stores kind ('md'|'csv'), pathFor takes kind, listDocs scans both extensions, readDoc/writeDoc derive kind via findOnDisk. createDoc(title, kind, content?) seeds kind-specific default ('# title' for md, 'title,status,notes' header for csv).
- src/lib/api.ts: Doc/DocMeta gained kind; api.create signature updated.
- src/index.ts: POST /api/docs accepts kind; agent.ts updated to keep create_doc tool md-only.
- src/lib/csv.ts: RFC-4180 parser/serializer (handles quoted fields with commas, embedded quotes, embedded newlines), findColumn helper.
- src/components/CsvEditor.tsx: editable table with +Row/+Column, per-row/-col delete, debounced onSave (600ms), idempotent serialize roundtrip.
- src/components/Editor.tsx: now dispatches to MarkdownEditor or CsvEditor by doc.kind.
- src/index.css: .csv-scroll, .csv-table, .csv-input styling.

Verified:
- bun src/index.ts boots; created md and csv via API curl, listed both, PUT tricky content (commas, embedded quotes), read-back content byte-identical, file on disk has correct extension. Round-trip parse→serialize→parse is idempotent.
