# Scaffold
tinybook is a small agent that helps write and manage markdown files.

## Tasks
- [ ] create a server side rendered app with bun
- [ ] create a markdown editor component
- [ ] create a sidebar for managing and selecting markdown documents
- [ ] create an agent harness with tools for editing and creating markdown documents
- [ ] use a basic file based data structure
- [ ] create an agent chat pane
- [ ] create a settings modal for setting up the agent
- [ ] allow user to utilize the mercury models from Inception Labs

<!--
Spec format reference (full docs in the printer README):
  * Lines starting with `- [ ]`, `- [x]`, `* [ ]`, `+ [ ]` (etc.) at
    column 0 are tasks. The text after the checkbox is the title.
  * Lines indented by 2 spaces or one tab below a task become its
    description body.
  * Any unindented non-task line ends the current task's description.
  * Re-runs of `printer run <this-file>` are idempotent — items are
    matched to existing tasks by a stable anchor derived from this
    file's path + the task title.
-->
