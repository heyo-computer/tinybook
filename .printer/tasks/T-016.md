+++
id = "T-016"
title = 'add "@" mentions for existing files to the agent'
status = "done"
priority = 3
created_at = "2026-05-01T22:38:53Z"
updated_at = "2026-05-01T22:47:33Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "37ebd58a73b0a7b3"
+++
## Notes

- 2026-05-01T22:39:32Z [sarocu] Plan:
- src/components/Chat.tsx
  - Add `@`-trigger autocomplete on the textarea: track caret pos, when user types `@` (preceded by start/whitespace) open a small popover listing matching `DocMeta.slug`/`title` filtered by the suffix. Use `↑/↓/Enter/Tab` to select, Esc to close.
  - Pass `docs: DocMeta[]` prop down from `App` (it already has the list) so we don't duplicate the fetch. Update Chat `Props`.
  - On select, replace the `@query` token with `@<slug>` (keep raw text) and store the chosen slug in a `mentions: string[]` set for the current message.
- src/lib/api.ts + src/server/agent.ts
  - Extend `chat` request body with optional `mentions?: string[]`. In `runAgent`, when `mentions` is non-empty, prefetch docs via `readDoc` and inject their content as a fenced block into a synthetic system-after-prompt message (or as a `user` preface) so the model sees the file content without needing a `read_doc` tool call.
  - Cap injected content (e.g. 16k chars per doc) and skip missing slugs gracefully.
- src/App.tsx: pass `docs` to `<Chat>`.
- Verify: build (`bun build`), type-check, manual: type "summarize @", pick a doc, confirm the agent references its content in reply.
- 2026-05-01T22:45:03Z [sarocu] Refinement after re-reading src/components/Chat.tsx + src/server/agent.ts:

- Chat already takes `settings` only — add `docs: DocMeta[]` prop (App.tsx:291 already has the list). Pass and consume.
- Mentions UX: simple popover anchored above the textarea; track `caretIndex` via ref + onSelect/onChange. Match `@(\w[\w-]*)$` against the text up to caret. Filter by slug or lowercased title.
- Maintain `mentions: string[]` derived per-send by re-scanning `text` for `@<slug>` tokens whose slug exists in `docs`. Avoids managing stale state on backspace.
- Wire body: extend `api.chat` body type with `mentions?: string[]`; agent.ts `AgentRequest` adds `mentions?: string[]`.
- In `runAgent`: when `mentions?.length`, after the system message inject one `user` message: "Referenced documents (read-only context):\n\n" + per-doc fenced block "## <slug>\n\n```\n<content slice 0..16000>\n```". Skip slugs that `readDoc` returns null for.
- Verify: `bun run` server, type a message containing `@<slug>`, confirm reply references content; also confirm a typed `@nonexistent` is silently dropped (no 500).
- 2026-05-01T22:47:33Z [sarocu] Implemented: Chat.tsx now takes docs prop, renders @-mention popover (↑/↓/Enter/Tab/Esc), passes mentions[] in api.chat. Server agent.ts AgentRequest extended with mentions; injects fenced doc blocks (capped at 16k chars/doc) just before the last user message. App.tsx passes docs to Chat. CSS styling added for .mention-popover. Verified: tsc --noEmit clean, bun build bundles 47 modules without errors.
